{% extends "layout" %} {% block content %}

<h1>A jelenlegi vezetők</h1>


<table style="width:100%">
{% for leader in leaders %} 
  <tr>
    <th>{{ leader.name }}</th>
    
    <form action="/leader/{{ leader.id }}/delete" method="POST" id="deleteDormform">
        {{ csrfField }}
        <th><button type="submit" class="btn btn-danger">Törlés</button></th> 
    </form>
  </tr>
   {% endfor %}
</table>

<h1>Vezető létrehozása</h1>


<form class="form-horizontal" action="/leader" method="POST">
    {{ csrfField }}
    <div class="form-group">
        <label for="inputName" class="col-sm-2 control-label">Kollégiumi vezető neve</label>
        <div class="col-sm-10">
            <input type="text" class="form-control" id="inputName" name="name" value="{{ old('name') }}">
        </div>
    </div>

    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" class="btn btn-success">Mentés</button>
        </div>
    </div>
</form>

{% endblock %}